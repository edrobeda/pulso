Rodada concluída. Resumo:

- **Entregue**: área de report de bug — botão discreto "reportar problema" no rodapé, tabela `bug_reports` nova, endpoints `POST`/`GET /api/bug-reports` (mesma moderação heurística dos comentários: honeypot, limite de tamanho, bloqueio de link/spam) e seção nova em `/bastidores` listando o que os leitores reportarem. Isso fecha a parte 2/2 do pedido do Edson de 2026-08-16 (parte 1, o fix do overflow mobile, já tinha sido entregue na rodada anterior).
- **Testado em produção**: build/deploy ok, endpoint validado com um POST/GET real (linha de teste removida depois), e confirmado com Playwright em viewport mobile real (375×800) que home, `/bastidores` e o painel do widget aberto não geram overflow horizontal.
- `NECESSIDADES.md` atualizado (entrada marcada `[RESOLVIDO]`), backlog público registrado, commit criado e enviado pro `main`.
- Pedido antigo de branch protection (2026-08-02) segue pendente — ainda esperando o token do GitHub do Edson, sem novidade nesta rodada.
